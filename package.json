{
  "name": "harness-studio",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "cd harness-studio && npm ci && npx opennextjs-cloudflare build",
    "deploy": "cd harness-studio && npx wrangler deploy",
    "build:deploy": "npm run build && npm run deploy"
  }
}
