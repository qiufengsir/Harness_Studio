{
  "name": "harness-studio",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "cd harness-studio && npm install && npm run build:worker",
    "start": "cd harness-studio && npm start"
  }
}
